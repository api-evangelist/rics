generated: '2026-07-26'
method: generated
source: openapi/rics-digitalcommunity-api-openapi.json
note: >-
  RICS publishes no AGENTS.md and no skill files of its own - none were found on any RICS property.
  These three skills were generated by API Evangelist from the published DigitalCommunity OpenAPI
  and the conventions, errors and data-model artifacts in this repository. Every step is grounded in
  a real method+path pair from the specification; the specification declares no operationIds, so no
  operationId is quoted (see overlays/rics-digitalcommunity-api-overlay.yaml for the ones API
  Evangelist assigned).
skills:
- file: rics-authenticate-and-read-regulation-scheme.md
  name: Authenticate with RICS and read a firm's regulation scheme
  api: openapi/rics-digitalcommunity-api-openapi.json
  operations: ['POST /token', 'GET /api/Regulation/{schemeNumber}']
- file: rics-check-firm-subscriptions-and-billing.md
  name: Check a RICS firm's subscriptions and billing position
  api: openapi/rics-digitalcommunity-api-openapi.json
  operations: ['POST /token', 'GET /api/Regulation/Subscriptions', 'GET /api/Regulation/PaymentInformation', 'GET /api/Payment/{id}', 'GET /api/Payment/{id}/reference/{reference}']
- file: rics-validate-survey-writer-licence.md
  name: Validate a RICS survey-writer software licence
  api: openapi/rics-digitalcommunity-api-openapi.json
  operations: ['POST /token', 'GET /api/SurveyWriter/{id}']
not_covered:
  writes: >-
    No skill is offered for POST /api/Payment/update, POST /api/OlaMerchantPost or the AzureStorage
    document and log operations. The payment writes have no idempotency contract, OlaMerchantPost
    has no published contract at all, and two AzureStorage operations are destructive - these belong
    behind a human decision, not in an agent skill. See agentic-access/rics-agentic-access.yml.
